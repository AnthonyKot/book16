# Receipts — ch. 02 log4j2 (Log4Shell, CVE-2021-44228)

Verbatim outputs against a full clone of https://github.com/apache/logging-log4j2.
Regenerate with `scripts/receipts-02.sh`. Long outputs truncated editorially (head).

## R1 — 2010: variable substitution arrives, with a plugin loader
```
$ git -C repos/log4j2 show --format=fuller --stat 2338fcb9fb
commit 2338fcb9fb5cff5325305db4da1bed098b930beb
Author:     Ralph Goers <rgoers@apache.org>
AuthorDate: Mon Oct 25 05:09:08 2010 +0000
Commit:     Ralph Goers <rgoers@apache.org>
CommitDate: Mon Oct 25 05:09:08 2010 +0000

    Add support for variable substitution
    
```

## R2 — 2012: the pivot — 'to improve performance', %m now interpolated
```
$ git -C repos/log4j2 show --format=%H%n%an%n%aI%n%s 8916625d91
8916625d9141f9c1e8c52e09d949d36fbe2555df
Ralph Goers
2012-09-23T21:27:17+00:00
Move variable substitution from PatternLayout to appropriate converters to improve performance.

diff --git a/core/src/main/java/org/apache/logging/log4j/core/layout/PatternLayout.java b/core/src/main/java/org/apache/logging/log4j/core/layout/PatternLayout.java
```

```
$ git -C repos/log4j2 show 8916625d91 -- '*MessagePatternConverter.java' | grep -A1 'contains'
+            toAppendTo.append(config != null && result.contains("${") ?
+                config.getSubst().replace(event, result) : result);
```

## R3 — 2013: one lookups.put('jndi'...) and the documented colon
```
$ git -C repos/log4j2 show --format=%H%n%an%n%aI%n%s f1a0cac60f
f1a0cac60f1e41347c9bced7c1470be488840344
Ralph Goers
2013-07-18T19:47:20+00:00
LOG4J2-313 - Add JNDILookup

diff --git a/core/src/main/java/org/apache/logging/log4j/core/lookup/Interpolator.java b/core/src/main/java/org/apache/logging/log4j/core/lookup/Interpolator.java
```

```
$ git -C repos/log4j2 show f1a0cac60f | grep -E 'lookups.put..jndi|indexOf..:... == -1|no prefix will be added'
+        lookups.put("jndi", new JndiLookup());
+            java:comp/env/, however if the key contains a ":" no prefix will be added.
```

## R4 — 2016: the off switch is added (default off)
```
$ git -C repos/log4j2 show --format=fuller --stat 6a4c88d4ae
commit 6a4c88d4ae85f6a70843df23e0781becc790c821
Author:     Gary Gregory <ggregory@apache.org>
AuthorDate: Fri Aug 19 00:31:44 2016 -0700
Commit:     Gary Gregory <ggregory@apache.org>
CommitDate: Fri Aug 19 00:31:44 2016 -0700

    [LOG4J2-905] Ability to disable (date) lookup completely, compatibility
    issues with other libraries like Camel.
```

## R5 — 2017: the JVM-wide kill switch, default false
```
$ git -C repos/log4j2 show dd18e9b210 -- '*Constants.java' | grep -A1 FORMAT_MESSAGES_PATTERN_DISABLE_LOOKUPS
+    public static final boolean FORMAT_MESSAGES_PATTERN_DISABLE_LOOKUPS = PropertiesUtil.getProperties().getBooleanProperty(
+            "log4j.formatMsgNoLookups", false);
```

## R6 — 2021, eleven days before the CVE: 'this feature is not used'
```
$ git -C repos/log4j2 log -1 --format=%H%nAuthorDate:\ %aI%nCommitDate:\ %cI 001aaada7d
001aaada7dab82c3c09cde5f8e14245dc9d8b454
AuthorDate: 2021-11-29T14:20:33+00:00
CommitDate: 2021-12-04T22:47:24-07:00
```

```
$ git -C repos/log4j2 log -1 --format=%B 001aaada7d
LOG4J2-3198: Log4j2 no longer formats lookups in messages by default

Lookups in messages are confusing, and muddy the line between logging APIs
and implementation. Given a particular API, there's an expectation that a
particular shape of call will result in specific results. However, lookups
in messages can be passed into JUL and will result in resolved output in
log4j formatted output, but not any other implementations despite no direct
dependency on those implementations.

There's also a cost to searching formatted message strings for particular
escape sequences which define lookups. This feature is not used as far as
we've been able to tell searching github and stackoverflow, so it's
unnecessary for every log event in every application to burn several cpu
cycles searching for the value.
```

## R7 — the fire: JndiExploit.java checked in with the first limiting patch
```
$ git -C repos/log4j2 show d82b47c6fa --stat
commit d82b47c6fae9c15fcb183170394d5f1a01ac02d3
Author: Ralph Goers <rgoers@apache.org>
Date:   Sat Dec 4 21:00:58 2021 -0700

    LOG4J2-3201 - Limit the protocols JNDI can use by default. Limit the servers and classes that can be accessed via LDAP.

 log4j-core/pom.xml                                 |   5 +
 .../apache/logging/log4j/core/net/JndiManager.java | 120 ++++++++++++++++-
```

```
$ git -C repos/log4j2 show d82b47c6fa -- '*JndiExploit.java' | grep -E 'class JndiExploit|fail'
+public class JndiExploit implements ObjectFactory {
+        fail("getObjectInstance must not be allowed");
```

## R8 — 2.16 excludes JndiLookup by hard-coded FQCN (the plugin loader would resurrect it)
```
$ git -C repos/log4j2 show 70edc23334 -- \*Interpolator.java
commit 70edc233343815d5efa043b54294a6fb065aa1c5
Author: Ralph Goers <rgoers@apache.org>
Date:   Mon Dec 13 20:10:27 2021 -0700

    Disable JNDI Lookup and Lookups in MessagePatternConverter

diff --git a/log4j-core/src/main/java/org/apache/logging/log4j/core/lookup/Interpolator.java b/log4j-core/src/main/java/org/apache/logging/log4j/core/lookup/Interpolator.java
index 72e368ff5c..01ee511bbe 100644
--- a/log4j-core/src/main/java/org/apache/logging/log4j/core/lookup/Interpolator.java
+++ b/log4j-core/src/main/java/org/apache/logging/log4j/core/lookup/Interpolator.java
@@ -73,7 +73,9 @@ public class Interpolator extends AbstractConfigurationAwareLookup {
         for (final Map.Entry<String, PluginType<?>> entry : plugins.entrySet()) {
```

## R9 — the PluginManager TODO that made the FQCN exclude necessary (2014-2022)
```
$ git -C repos/log4j2 log --all --format=%h\ %ad\ %an\ %s --date=short -S this\ ought\ to\ use\ the\ PluginManager -- \*.java
c062f55318 2022-01-21 Carter Kozak LOG4J2-3358: Fix substitutions when programmatic configuration is used (#726)
7ca3c4f5f2 2022-01-21 Carter Kozak LOG4J2-3358: Fix substitutions when programmatic configuration is used (#726)
c44856afae 2014-05-04 mattsicker Initial migration of servlet code to log4j-web.
```

