# Receipts — ch. 10 tensor2tensor (the Transformer's repository)

Verbatim outputs against a full clone of tensorflow/tensor2tensor.
Regenerate with `scripts/receipts-10.sh`. Long outputs truncated editorially (head).

## R1 — day one: a title, then 155 seconds later, the Transformer
```
$ git -C repos/tensor2tensor log --reverse --format='%h %ai %an %s' | head -3
cedf0e2d 2017-06-15 11:24:53 -0700 Ryan Sepassi first commit
3d9c62f2 2017-06-15 11:27:28 -0700 Ryan Sepassi initial push
414cee3e 2017-06-15 13:26:15 -0700 Lukasz Kaiser Merge pull request #1 from rsepassi/master
```

```
$ git -C repos/tensor2tensor show --stat --format= cedf0e2df6 | tail -2
 README.md | 1 +
 1 file changed, 1 insertion(+)
```

```
$ git -C repos/tensor2tensor show --shortstat --format= 3d9c62f2ac | tail -1
 74 files changed, 15315 insertions(+), 1 deletion(-)
```

## R2 — what the initial push already contains
```
$ git -C repos/tensor2tensor show 3d9c62f2ac:tensor2tensor/models/transformer.py | sed -n '15,20p'
"""transformer (attention).

encoder: [Self-Attention, Feed-forward] x n
decoder: [Self-Attention, Source-Target-Attention, Feed-forward] x n

"""
```

```
$ git -C repos/tensor2tensor show 3d9c62f2ac:tensor2tensor/models/transformer.py | wc -l
495
```

```
$ git -C repos/tensor2tensor show 3d9c62f2ac:AUTHORS | tail -2

Google Inc.
```

## R3 — who commits, and who never does
```
$ git -C repos/tensor2tensor shortlog -sn HEAD
   765	T2T Team
   673	Lukasz Kaiser
   448	Ryan Sepassi
   359	Afroz Mohiuddin
   182	Piotr Kozakowski
   166	Noam Shazeer
   157	piotrmilos
   143	Mohammad Babaeizadeh
```

```
$ git -C repos/tensor2tensor shortlog -sn HEAD | grep -E 'Vaswani|Parmar|Gomez'
    92	Niki Parmar
    57	Ashish Vaswani
    30	Aidan Gomez
```

```
$ git -C repos/tensor2tensor log --author='Uszkoreit' --oneline | wc -l
0
```

```
$ git -C repos/tensor2tensor log --author='Polosukhin' --oneline | wc -l
0
```

```
$ git -C repos/tensor2tensor log --author='Llion' --oneline | wc -l
0
```

## R4 — week one: the projection shows the office
```
$ git -C repos/tensor2tensor show --stat --format=fuller 01787ca8a5 | head -10
commit 01787ca8a53e96c56eb6826443c5a12a29e9209a
Author:     Ryan Sepassi <rsepassi@google.com>
AuthorDate: Wed Jun 21 12:22:25 2017 -0700
Commit:     Ryan Sepassi <rsepassi@google.com>
CommitDate: Wed Jun 21 14:42:53 2017 -0700

    Add leakr dictionary to prevent internal stuff leaking out
    
    PiperOrigin-RevId: 159726434

```

```
$ git -C repos/tensor2tensor show 01787ca8a5 | grep -E '^[+-]' | grep -iE 'blaze|brain' | head -3
-./blaze-bin/third_party/py/tensor2tensor/data_generators/replace_oov \
-      (see learning/brain/google/python/ops/candidate_sampling.py)
-      (see learning/brain/google/python/ops/candidate_sampling.py)
```

## R5 — the look_right timeline, newest first
```
$ git -C repos/tensor2tensor log -S look_right --format='%h %ai %an %s' -- '*.py'
a0bd0177 2017-08-04 14:41:57 -0700 Ashish Vaswani Reverted back to the previous masked_local_attention_1d because the current one was giving 0 losses indicating that it was peeking into the future. The way the attention bias was being added also seemed wrong. Renamed unmasked_local_attention_1d to local_attention_1d. The user can specify local_attention_1d if they want to look left and right of the query block.
75270907 2017-08-01 18:03:10 -0700 Lukasz Kaiser More WMT corrections, port image problems to Problem.
33d47f0a 2017-07-27 23:46:12 -0700 Niki Parmar Add tests for dot product and local unmasked attention
2ced78db 2017-08-01 11:08:24 +0100 William Unify methods and started work on Bias
37e7dedf 2017-07-28 14:06:55 +0100 William Updated sliding window
```

## R6 — the unsafe signature arrives inside a commit about something else
```
$ git -C repos/tensor2tensor show 75270907db -- tensor2tensor/models/common_attention.py | grep -B1 -A2 'masked_local_attention_1d' | head -8
 
-def masked_local_attention_1d(
-    q, k, v, block_length=128, name=None):
-  """Attention to the source position and a neigborhood to the left of it.
+def masked_local_attention_1d(q, k, v,
+                              block_length=128, look_right=True,
+                              use_whole_block=False, name=None):
--
```

## R7 — the file changes address while the bug is live
```
$ git -C repos/tensor2tensor show --summary --format='%h %ai %an %s' f6799b9515 -- tensor2tensor/models/common_attention.py tensor2tensor/layers/common_attention.py
f6799b95 2017-08-02 16:45:57 -0700 Ryan Sepassi File/code moves

 rename tensor2tensor/{models => layers}/common_attention.py (89%)
```

## R8 — the first author of the paper reads the loss curve
```
$ git -C repos/tensor2tensor show -s --format=fuller a0bd0177bf | head -16
commit a0bd0177bf766c953041b7451398ab1791adb1e5
Author:     Ashish Vaswani <avaswani@google.com>
AuthorDate: Fri Aug 4 14:41:57 2017 -0700
Commit:     Ryan Sepassi <rsepassi@google.com>
CommitDate: Fri Aug 4 16:14:33 2017 -0700

    Reverted back to the previous masked_local_attention_1d because the current one was giving 0 losses indicating that it was peeking into the future. The way the attention bias was being added also seemed wrong. Renamed unmasked_local_attention_1d to local_attention_1d. The user can specify local_attention_1d if they want to look left and right of the query block.
    
    PiperOrigin-RevId: 164312109
```

```
$ git -C repos/tensor2tensor show a0bd0177bf -- tensor2tensor/layers/common_attention.py | grep -E '^[+-].*look_right' | head -3
-                              look_right=True,
-  If look_right is True then each query will attend to block_length//2
-  meaning the full blocks are used (if look_right is True then the elements to
```

## R9 — day 16: correctness wins, and says what it costs
```
$ git -C repos/tensor2tensor show -s --format=fuller 98be812807 | head -12
commit 98be8128078b8a8da34271eea60ad88f879389d7
Author:     Noam Shazeer <noam@google.com>
AuthorDate: Fri Jun 30 14:41:03 2017 -0700
Commit:     lukaszkaiser <lukaszkaiser@google.com>
CommitDate: Sat Jul 1 15:16:48 2017 -0700

    Fix subword_text_tokenizer to make it invertible again.   This breaks existing models and vocabularies.  Change criteria for which characters are parts of words and which are separators - we now consider unicode letters and numbers to be parts of words.
    
    PiperOrigin-RevId: 160690718
```

## R10 — 2019: a sincere one-line fix to the most famous formula in the library
```
$ git -C repos/tensor2tensor show -s --format='%h %ai %an %s' e3e54900d3
e3e54900 2019-12-12 16:20:46 +0800 geek_fly inaccurate calculation of first token in pos_emb (#1758)
```

```
$ git -C repos/tensor2tensor show e3e54900d3 -- tensor2tensor/layers/common_attention.py | grep -E '^[+-].*position' | head -2
-  position = tf.to_float(tf.range(length) + start_index)
+  position = tf.to_float(tf.range(1, 1+length) + start_index)
```

## R11 — nine days later: compatibility wins, and says why
```
$ git -C repos/tensor2tensor show --stat --format=fuller e4ea462e73 | head -18
commit e4ea462e73388b6c8a2ea99a5d92aacd9c3237c5
Author:     T2T Team <no-reply@google.com>
AuthorDate: Sat Dec 21 20:59:49 2019 -0800
Commit:     Copybara-Service <copybara-worker@google.com>
CommitDate: Sat Dec 21 21:00:26 2019 -0800

    *** Reason for rollback ***
    
    This change will break existing checkpoints in a hard to debug, hard to notice manner. Existing checkpoints will still load and run, but produce entirely incorrect results.
    
    *** Original change description ***
    
    PR Title: inaccurate calculation of first_token's pos_embedding
    
    PR Body:
    since,
    1. scaled_time is calculated by `scaled_time = tf.expand_dims(position, 1) * tf.expand_dims(inv_timescales, 0)`, while position_idx in position will **start by 0**.
    2. scaled_time will be used to calculate position embedding by `signal = tf.concat([tf.sin(scaled_time), tf.cos(scaled_time)], axis=1)`
```

## R12 — the TODO that outlived both names
```
$ git -C repos/tensor2tensor log -S 'ask rsepassi' --format='%h %ai %an %s'
8c4893ca 2018-08-10 12:31:35 -0700 Aidan Gomez Update adversarial attacks in T2T
6d9274e1 2018-04-15 23:19:49 -0700 Noam Shazeer Improvements to bfloat16 weights option.
```

```
$ git -C repos/tensor2tensor blame -L 250,251 tensor2tensor/bin/t2t_trainer.py
6d9274e12 (Noam Shazeer 2018-04-15 23:19:49 -0700 250)   # the various custom getters we have written do not play well together yet.
6d9274e12 (Noam Shazeer 2018-04-15 23:19:49 -0700 251)   # TODO(noam): ask rsepassi for help here.
```

```
$ git -C repos/tensor2tensor log --author=Sepassi -1 --format='%ai %s'
2019-03-07 10:29:13 -0800 Update trax_test to test_train_eval_predict
```

```
$ git -C repos/tensor2tensor log --author=Shazeer -1 --format='%ai %s'
2019-09-12 21:27:59 -0700 increase window size of partially-built sequences in c++ packing op - results in more efficient packing.
```

## R13 — the fade: one word, then a timer
```
$ git -C repos/tensor2tensor log --format='%ad' --date=format:%Y | sort | uniq -c | tail -7
    850 2017
   2228 2018
   1192 2019
     75 2020
     16 2021
     14 2022
      4 2023
```

```
$ git -C repos/tensor2tensor show 302a04f0e4 -- README.md | grep -E '^[+-].*mode|^[+-].*deprecated' | head -2
-of users. It is now in maintenance mode &mdash; we keep it running and welcome
+of users. It is now deprecated &mdash; we keep it running and welcome
```

```
$ git -C repos/tensor2tensor log -1 --format='%h %ai %an %s'
bafdc1b6 2023-04-01 03:18:41 -0700 T2T Team Internal change
```

