You are scouting one public git repository for a book of git archaeology. Read ~/book16/scout/BRIEF.md first and follow it.

Repository: https://github.com/FFmpeg/FFmpeg  ->  clone (full history, no --depth) into ~/book16/repos/ffmpeg if not already present. Use `git -C ~/book16/repos/ffmpeg ...` for everything. Do not modify the repo. Do not write anywhere except ~/book16/scout/ffmpeg.md.

Method — actually dig, don't recite folklore:
- git log --reverse | head, first commits and their messages/dates/authors (odd dates, planted commits, imported history boundaries).
- git log --diff-filter=... / --stat for the smallest commits with the biggest later consequences; git log -S / -G for known-story strings, but verify every hash by running the command.
- Repo-specific leads (verify, don't trust): The 2011 split: a group of maintainers forked to Libav; Michael Niedermayer then merged Libav's work back into FFmpeg near-daily for YEARS — the remote was literally called 'qatar' (git log --grep=qatar). Find the first and the last qatar merge and what ending them said; the Jan 2011 takeover moment if visible in commits; Fabrice Bellard's first commits (2000) and disappearance; Niedermayer's commit share over time.
- git shortlog -sn over time windows: names that appear/disappear; maintainers handing over.
- git blame on a few very old files to find lines/comments that survived; git log -L or -S for their origin.
- Timestamps: commits at odd hours/dates, commit date vs author date gaps.
- Any find must include the exact reproducible command and its output excerpt.

Write ~/book16/scout/ffmpeg.md with: repo, clone size, history span (first/last commit dates), number of commits; then 5-12 candidate digs, each with: title, find-type (1-8 from brief), hash(es), date, author, message, the git command that shows it (and 3-10 lines of its output), why it is a story (one paragraph, moment-before-the-outcome), what the reader learns about their own repos, surprise 1-5. End with a ranked top-3 and one honest line about what was disappointing.

Return as your final message: the ranked top-3 in 6 lines total, plus history span and commit count. Nothing else.
